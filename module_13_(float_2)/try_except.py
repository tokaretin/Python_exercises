# 🔰 Что такое try?
# try используется, чтобы "попробовать" выполнить код, который может вызвать ошибку.

try:
    # Пробуем преобразовать ввод в целое число
    number = int(input("Введите целое число: "))
    print("Вы ввели число:", number)
except:
    # Если возникла ошибка (например, ввели не число), выполняется этот блок
    print("Ошибка: вы ввели не число!")

# 📌 Как это работает?
# 1. Пользователь вводит данные.
# 2. Если всё хорошо — работает блок try.
# 3. Если ошибка — программа переходит в блок except и не ломается.

# 💡 Зачем это нужно?
# Чтобы программа не завершалась с ошибкой при неправильном вводе.

# 👇 Можно также обрабатывать конкретные ошибки:
try:
    num = float(input("Введите число с точкой (например, 1.5): "))
    print("Вы ввели:", num)
except ValueError:
    print("Ошибка: нужно ввести число, например 2.3")

# 🔑 Ключевые слова:
# try     - попробовать выполнить код
# except  - выполнить, если была ошибка
# int()   - преобразование к целому числу
# float() - преобразование к числу с точкой
# ValueError - тип ошибки, когда ввод не число